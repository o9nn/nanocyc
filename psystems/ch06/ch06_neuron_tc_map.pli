/* ============================================================================
 * psystems/ch06/ch06_neuron_tc_map.pli
 *
 * NanoBrain CH06: Unprecedented technologies of nature led by harvesting the geometry of singularity
 * Issue #11 — P-Lingua specification (see psystems/README.md).
 *
 * Sections: 6.8
 * Functionality: Measurement of a complete time-crystal map of a neuron.
 * Traceability: src/cpp/nanobrain_singularity.h
 * Initial multiset: sample objects in each membrane so the simulator can run
 *                   at least one evolution step.
 * ==========================================================================*/

@model<psystems_basic>

/* Membrane structure: skin with one membrane per functional unit. */
@mu = [ []'probe []'map ]'skin;

@mskin = seed;
@mprobe = unit(probe), fuel;
@mmap = unit(map), fuel;

/* --------------------------------------------------------------------------
 * Evolution rules
 * seed_dispense : the skin injects a work token into each functional membrane
 * step_%s-local : each membrane transforms its unit object and consumes fuel
 * report        : finished results are sent out to the skin
 * ------------------------------------------------------------------------*/
[seed_dispense : seed --> (work)in_probe ]'skin;
[seed_dispense : seed --> (work)in_map ]'skin;
[step : work * unit(probe) * fuel --> done(probe) ]'probe;
[step : work * unit(map) * fuel --> done(map) ]'map;
[report : done(probe) --> (result(probe))out ]'probe;
[report : done(map) --> (result(map))out ]'map;
