/* ============================================================================
 * psystems/ch03/ch03_ppm_metric2_orderedfactor.pli
 *
 * NanoBrain CH03: Phase prime metric (PPM) links all symmetries
 * Issue #8 — P-Lingua specification (see psystems/README.md).
 *
 * Sections: 3.3
 * Functionality: Metric 2: product of primes forming integers leads to a unique ordered-factor metric.
 * Traceability: src/cpp/nanobrain_ppm.h, nanobrain_ppm.cpp
 * Initial multiset: sample objects in each membrane so the simulator can run
 *                   at least one evolution step.
 * ==========================================================================*/

@model<psystems_basic>

/* Membrane structure: skin with one membrane per functional unit. */
@mu = [ []'metric2 ]'skin;

@mskin = seed;
@mmetric2 = unit(metric2), fuel;

/* --------------------------------------------------------------------------
 * Evolution rules
 * seed_dispense : the skin injects a work token into each functional membrane
 * step_%s-local : each membrane transforms its unit object and consumes fuel
 * report        : finished results are sent out to the skin
 * ------------------------------------------------------------------------*/
[seed_dispense : seed --> (work)in_metric2 ]'skin;
[step : work * unit(metric2) * fuel --> done(metric2) ]'metric2;
[report : done(metric2) --> (result(metric2))out ]'metric2;
