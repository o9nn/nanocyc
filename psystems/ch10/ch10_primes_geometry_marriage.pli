/* ============================================================================
 * psystems/ch10/ch10_primes_geometry_marriage.pli
 *
 * NanoBrain CH10: Uploading consciousness: the evolution of conscious machines of the future
 * Issue #15 — P-Lingua specification (see psystems/README.md).
 *
 * Sections: 10.7
 * Functionality: The marriage of primes with geometry would reshape humanity.
 * Traceability: src/cpp/nanobrain_consciousness.h, nanobrain_ontogenesis.h
 * Initial multiset: sample objects in each membrane so the simulator can run
 *                   at least one evolution step.
 * ==========================================================================*/

@model<psystems_basic>

/* Membrane structure: skin with one membrane per functional unit. */
@mu = [ []'primes []'geometry ]'skin;

@mskin = seed;
@mprimes = unit(primes), fuel;
@mgeometry = unit(geometry), fuel;

/* --------------------------------------------------------------------------
 * Evolution rules
 * seed_dispense : the skin injects a work token into each functional membrane
 * step_%s-local : each membrane transforms its unit object and consumes fuel
 * report        : finished results are sent out to the skin
 * ------------------------------------------------------------------------*/
[seed_dispense : seed --> (work)in_primes ]'skin;
[seed_dispense : seed --> (work)in_geometry ]'skin;
[step : work * unit(primes) * fuel --> done(primes) ]'primes;
[step : work * unit(geometry) * fuel --> done(geometry) ]'geometry;
[report : done(primes) --> (result(primes))out ]'primes;
[report : done(geometry) --> (result(geometry))out ]'geometry;
