/* ============================================================================
 * psystems/ch05/ch05_creativity_primes.pli
 *
 * NanoBrain CH05: Big data in the garden of gardens (GOG) of meander flower: universal time crystal
 * Issue #10 — P-Lingua specification (see psystems/README.md).
 *
 * Sections: 5.8
 * Functionality: How geometric similarity builds creativity in the computing of primes.
 * Traceability: src/cpp/nanobrain_gog.h, nanobrain_tc_transform.h, nanobrain_spontaneous.h
 * Initial multiset: sample objects in each membrane so the simulator can run
 *                   at least one evolution step.
 * ==========================================================================*/

@model<psystems_basic>

/* Membrane structure: skin with one membrane per functional unit. */
@mu = [ []'similarity []'create ]'skin;

@mskin = seed;
@msimilarity = unit(similarity), fuel;
@mcreate = unit(create), fuel;

/* --------------------------------------------------------------------------
 * Evolution rules
 * seed_dispense : the skin injects a work token into each functional membrane
 * step_%s-local : each membrane transforms its unit object and consumes fuel
 * report        : finished results are sent out to the skin
 * ------------------------------------------------------------------------*/
[seed_dispense : seed --> (work)in_similarity ]'skin;
[seed_dispense : seed --> (work)in_create ]'skin;
[step : work * unit(similarity) * fuel --> done(similarity) ]'similarity;
[step : work * unit(create) * fuel --> done(create) ]'create;
[report : done(similarity) --> (result(similarity))out ]'similarity;
[report : done(create) --> (result(create))out ]'create;
